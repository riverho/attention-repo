name: "E-ATTN-MCP-INDEX-01 pipeline"

on:
  pull_request:
    paths:
      - "mcp-server/index.js"
      - '!MAP.md'
  push:
    branches: [main]
    paths:
      - "mcp-server/index.js"
      - '!MAP.md'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Verify entity file exists
        run: |
          test -f "mcp-server/index.js" || {
            echo "ERROR: E-ATTN-MCP-INDEX-01 file mcp-server/index.js not found"
            exit 1
          }

      - name: Run Python tests
        run: |
          python3 -m unittest discover -s tests -p "test_*.py"

      - name: Run Node tests
        run: |
          npm install --silent
          node tests/test_package_contract.js
          node tests/test_bun_cli.js
          node tests/test_mcp_integration.js
